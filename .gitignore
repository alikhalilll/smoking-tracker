# Dependencies
node_modules
.pnpm-store

# Build output
dist
dist-ssr
*.local

# Environment / secrets — keep .env.example committed
.env
.env.local
.env.*.local
.env.development
.env.production

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS metadata
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# TypeScript / Vite caches
*.tsbuildinfo
.vite
.cache

# Test coverage
coverage
.nyc_output

# Local agent/editor config (per-user)
.claude/

# Supabase CLI local state — keep `supabase/functions/` and
# `supabase/config.toml` committed; the rest is per-machine scratch
# and may contain secrets.
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/functions/.env
supabase/functions/**/.env
